# Railway optimized npm configuration
fund=false
audit=false
progress=false
prefer-offline=true
cache=/tmp/.npm
# Disable package-lock updates during CI
package-lock=false
# Use faster install algorithm
install-strategy=shallow
